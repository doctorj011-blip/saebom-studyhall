# 좌석키 → 학생ID(uid) 이관 설계서

작성 2026-08-04 · 대상 저장소 `saebom-studyhall` (면학관 관리앱·학생앱·학부모앱 + Functions)

---

## 1. 왜 하는가

지금 이 시스템에서 학생을 가리키는 값은 **좌석번호**와 **이름** 두 가지다. 둘 다 바뀐다.

- **좌석은 교환·재배정된다.** 그런데 좌석 교환(`_swapSeatDocs`)은 `students`·`schedules`·`schedule_base`
  세 컬렉션만 옮긴다. 나머지 좌석키 컬렉션은 그 자리에 남아, 새 주인의 기록으로 둔갑한다.
- **이름은 개명·동명이인 정리로 바뀐다.** 이름이 조회 키인 컬렉션이 10개가 넘어, 관리앱의 이름 수정만으로는
  과거 기록이 끊긴다(현재는 수동 REST 이관으로 메꾸고 있다).

2026-08-04 운영 데이터 실측 결과 좌석 5곳에서 **37건이 실제로 뒤섞여** 있었다.
같은 날 조회 경로에 이름 검증을 넣어(커밋 `708a7b5`) 노출은 막았지만, 그건 증상 차단이다.
데이터는 여전히 잘못된 자리에 있고, 좌석·이름이 바뀔 때마다 같은 문제가 다시 생긴다.

**목표: 변하지 않는 학생 식별자 `uid`를 도입하고, 좌석과 이름을 단순 속성으로 강등한다.**

상용화(타 학원 SaaS 판매) 관점에서도 이건 선행 조건이다. 좌석 재배정이 잦은 학원에 지금 구조를
그대로 팔면 첫 달에 사고가 난다.

---

## 2. 현재 실태 (2026-08-04 실측)

### 2.1 규모

전체 **28,417건**. 이관 대상은 학생에 귀속된 기록이다.

| 컬렉션 | 건수 | 문서ID | 학생 식별 필드 |
|---|---:|---|---|
| `students` | 63 | **좌석** | name, seat |
| `schedules` | 50 | **좌석** | name, seat |
| `schedule_base` | 50 | **좌석** | name, seat |
| `daily_reports` | 527 | **날짜_좌석** | name, seat |
| `planners` | 506 | **좌석_날짜** | name, seat |
| `planner_ai_reviews` | 500 | **좌석_날짜** | name, seat |
| `planner_comments` | 456 | **좌석_날짜** | ⚠️ seat만 (이름 없음) |
| `penalties` | 142 | **날짜_좌석** | name, seat, seatKey |
| `merits` | 152 | 임의ID | name, seatKey |
| `attendance_sessions` | 1,800 | 임의ID | **studentName**, seat |
| `checkin_logs` | 6,223 | 임의ID | **studentName**, seat |
| `sms_logs` | 4,545 | 임의ID | ⚠️ **student** (필드명 다름) |
| `usage_logs` | 12,870 | 임의ID | name, seat |
| `studyroom_requests` | 456 | 임의ID | name, seat |
| `current_sessions` | 0 | ⚠️ **이름** | (입실 중에만 존재) |
| `student_memos` | 13 | ⚠️ **이름** | name |
| `withdrawn_students` | 5 | 임의ID | name, student, seat |
| `consultations` | 22 | 임의ID | name |
| `feedback_events` | 35 | 임의ID | ⚠️ seat만 (이름 없음) |
| `push_tokens` | 403 | 토큰 | ❓ 규칙상 미인증 조회 불가 |

`students` 63건 중 9건은 `_meta_*` 설정 문서다(실제 학생 54명).

### 2.2 부여 대상 인원

- 현재 명부 **54명**
- 이력에만 등장하는 이름(퇴원생 등) **22명**
- → uid를 부여할 사람 **약 76명**

### 2.3 이름만으로 사람을 못 가르는 경우 — 1건

동명이인 1쌍이 표기 3가지로 흩어져 있다(접미사 `(전화4자리)` 형태와 구분자 `A` 형태가 섞임).
**이 1건은 자동 판별이 불가능하며 수동 지정이 필요하다.** 나머지 75명은 표기가 일관된다.

### 2.4 이름 필드가 아예 없는 컬렉션

`planner_comments`(456건)와 `feedback_events`(35건)는 좌석만 있다. 다행히 `planner_comments`는
문서ID가 `좌석_날짜`라서 같은 키의 `planners`/`planner_ai_reviews`/`daily_reports`와 조인된다.

> 실측: **456건 전부 (좌석,날짜) 직접 조인 성공. 주인 확정 불가 0건.**

`feedback_events` 35건은 조인 근거가 약하다. 소량이므로 uid 없이 두고 후속 정리 대상으로 남긴다.

### 2.5 코드 밖 저장소

- **Firebase Storage**: 플래너 사진이 `planners/{좌석}/{날짜}.jpg` 경로에 있다.
  Firestore와 별개로 이관 대상이며, `listAll`이 좌석 폴더를 통째로 나열하는 구조라 노출 경로이기도 하다.
  ⚠️ **Storage 규칙이 이 저장소에 없다**(콘솔에서만 관리). 이관 전에 규칙 확인 필요.
- **`push_tokens`(403건)**: 이 저장소에 관련 코드가 없다. FCM 푸시는 별도 배포본에 있으므로
  **별도 조사가 필요하다.** 이 설계서 범위 밖.

---

## 3. 목표 구조

```
students/{uid}          ← 문서ID = 불변 uid. seat 은 그냥 필드.
  { uid, name, seat, phone, phoneLast4, ... }

daily_reports/{uid}_{날짜}
planners/{uid}_{날짜}
planner_ai_reviews/{uid}_{날짜}
planner_comments/{uid}_{날짜}
penalties/{uid}_{날짜}
merits/{임의ID}                 { uid, ... }
attendance_sessions/{임의ID}    { uid, ... }
current_sessions/{uid}
student_memos/{uid}
...
Storage: planners/{uid}/{날짜}.jpg
```

**uid 형식**: `s_` + 8자 랜덤(예: `s_k3n8fq2a`). 순번을 쓰지 않는 이유는 학원이 늘어날 때
충돌하지 않아야 하고, 순번은 사람이 추측·오타로 잘못 입력하기 쉽기 때문이다.

**좌석 → uid 역색인**은 두지 않는다. `students`를 통째로 읽는 화면(관리앱)이 이미 있고 54명 규모라
클라이언트에서 만드는 편이 단순하다. 키오스크·배치도는 메모리 맵으로 처리한다.

---

## 4. 단계별 계획

각 단계는 **독립적으로 배포 가능하고, 다음 단계 없이도 시스템이 정상 동작**해야 한다.
저장소 특성상 푸시가 곧 라이브 배포이므로 되돌릴 수 없는 단계를 뭉쳐서 올리면 안 된다.

### 0단계 — 안전망 (선행 필수)

- **PITR 활성화**. 현재 꺼져 있어 되돌릴 수단이 백업 export뿐이다. 이관 착수 전 반드시 켠다.
- 전 컬렉션 JSON export를 로컬에 보관(이관 스크립트가 자동 저장).
- Storage 규칙 확인(현재 저장소에 없음).

### 1단계 — uid 부여 (쓰기: `students`에 필드 추가만)

1. 현재 명부 54명 + 이력 22명 → 신원 목록 생성
2. 동명이인 1건은 **수동 확인 후 지정** (자동 판별 불가)
3. `students` 각 문서에 `uid` 필드 추가. 문서ID는 아직 좌석 그대로.
4. 전체 신원 목록(퇴원생 포함)을 레지스트리에 보관(2단계 백필이 이걸 쓴다)

이 단계는 기존 코드에 아무 영향이 없다. 필드가 하나 늘 뿐이다.

> **실행 완료 2026-08-04** — `scripts/assign-uid.js`
> 신원 76명(재적 54 + 이력전용 22), students 54건에 uid 부여, 필드 유실 0.
>
> 설계에서 바뀐 점 두 가지:
> - 레지스트리를 별도 컬렉션 `student_identities` 가 아니라 **`students/_meta_uid_registry`** 에 둔다.
>   `firestore.rules` 맨 아래 catch-all 이 신규 컬렉션을 전부 막고 규칙 배포는 수동이라,
>   이미 열려 있고 `_meta_*` 관례가 있는 `students` 를 쓰는 편이 단계가 적다.
>   ⚠️ 이 문서에 **truthy 한 `name` 필드를 두면 안 된다.** 3앱·Functions 전부 `if (!v.name)` 로
>   메타 문서를 걸러내므로 이름이 있으면 유령 학생이 된다(기존 메타 9개는 `name: ""` 로 통과 중).
> - 동명이인은 **좌석 동시등장**으로 갈랐다. 4개 표기가 각각 단 하나의 좌석에서만 등장하고
>   (박지윤·(9557) → 1번 / 박지윤A·(2271) → 10번) 다른 좌석에 섞인 건이 0건이라 근거가 깨끗하다.
>   다만 자동 확정하지 않고 계획파일에 사람이 적어 넣는 구조로 만들었다.

### 2단계 — 이력에 uid 백필 (쓰기: 각 기록에 `uid` 필드 추가)

이름으로 조인해 모든 기록에 `uid` 필드를 채운다. 문서ID는 아직 안 바꾼다.

- 이름 매칭은 이미 검증된 `_sameStudentName` 규칙 재사용
  (`홍길동(1234)`=`홍길동`, `홍길동`=`홍길동A` 흡수 — 운영 1,827건 대조 완료)
- `planner_comments`는 (좌석,날짜) 조인으로 채운다(456/456 성공 확인)
- 매칭 실패 건은 **건드리지 않고 목록으로 보고**한다. 추측해서 채우지 않는다.

여전히 기존 코드는 좌석으로 읽으므로 정상 동작한다. 되돌리려면 `uid` 필드만 지우면 된다.

> **실행 완료 2026-08-04** — `scripts/backfill-uid.js`
> 14개 컬렉션 **10,402건 전부 uid 채움 · 실패 0 · 미해결 0.**
> `planner_comments` 456건은 (좌석,날짜) 조인으로 100% 해결.
>
> **독립 검증**(스크립트가 아닌 별도 대조): uid 76개, 한 uid에 여러 사람 이름이 섞인 건 0,
> 한 표기가 여러 uid로 갈린 건 0, 동명이인은 두 uid로 정확히 분리됨.
>
> 🔴 **규칙상 백필이 불가능한 컬렉션 2개 — 3단계 설계에 영향**
> - `usage_logs`(12,870) : `allow update: if false`
> - `planner_ai_reviews`(500) : update 가 `summary`/`statsFixed` 로만 제한
>
> `planner_ai_reviews` 는 **3앱 모두 학생별로 조회하는 컬렉션**이라 그냥 넘길 수 없다. 선택지:
> (a) 규칙에 `uid` 추가를 허용하고 수동 배포 → 백필 후 uid 조회로 전환
> (b) 이 컬렉션만 좌석 조회 + 이름 검증(2026-08-04 핫픽스) 유지
> `usage_logs` 는 집계·분석용이고 학생별 조회 경로가 아니므로 (b) 로 둬도 무방하다.
>
> `sms_logs`(4,545)는 로그성이라 기본 대상에서 뺐다. 필요하면 `--include-sms`.

### 3단계 — 읽기 경로를 uid로 전환 (코드 변경, 데이터 무변경)

앱이 `uid` 필드로 조회하도록 바꾼다. 문서ID는 아직 좌석이다.

- `where('seatKey','==',seat)` → `where('uid','==',uid)`
- 좌석 복합키 `doc(...)` 직접 접근은 **uid 조회 + 폴백**으로 이중화
- 이 시점에 2단계 백필이 빠뜨린 기록이 있으면 화면에서 드러난다 → **여기서 충분히 관찰한 뒤 다음으로**

⚠️ 3앱을 동시에 배포해야 한다. 학생앱만 먼저 올리면 관리앱이 쓴 기록을 학생앱이 못 읽는다.

> **부분 완료 2026-08-04** — `planner_ai_reviews` 는 선택지 (b) 채택.
>
> **uid 조회로 전환한 것**
> - 학생앱·학부모앱: 벌점, 상점
> - 관리앱: 학생별 벌점 목록, 벌점 초기화(쓰기), 상점 모달
> - 관리앱 공부시간 재계산(`recomputeMonthHours`) — 이름 → uid
> - 학생앱 플래너 소유자 판정
>
> **의도적으로 좌석/이름을 유지한 것**
> - `planner_ai_reviews`(3앱) — 규칙상 uid 를 못 넣는다. 좌석 조회 + 이름 검증(`708a7b5`) 유지.
> - 학생앱 플래너 달력 — Storage 경로가 좌석 기준이라 조회 자체는 좌석이어야 한다(4·5단계에서 정리).
> - `attendance_sessions`·`checkin_logs` 의 나머지 학생별 조회(학생앱 2곳·학부모앱 2곳·관리앱 2곳)
>   — 좌석 혼입과 무관한 경로이고, uid 로 바꾸면 **복합 색인이 필요**하다. 아래 참조.
>
> 🔴 **색인 제약** — 이 프로젝트는 색인을 저장소로 관리하지 않는다(`firestore.indexes.json` 없음).
> 콘솔에서 그때그때 만들어 온 탓에, 새 2필드 쿼리는 색인이 없어 **배포 즉시 실패**한다.
> 그래서 `recomputeMonthHours` 는 `uid` 단일 조건으로 조회하고 월 필터를 클라이언트에서 한다
> (한 학생의 전체 세션은 수십 건이라 부담 없음 — 실측 52건).
> 남은 이름 기반 조회들을 uid 로 옮기려면 복합 색인을 먼저 만들어야 한다.
>
> **uid 유실 방지**(3단계에서 함께 넣음 — 이게 없으면 1·2단계 결과가 조용히 파괴된다)
> - `saveStudentToFirebase`·`_studentDocData` 는 `setDoc` 으로 문서를 통째로 덮는다 → uid 보존 + 없으면 발급
> - 예약 승계·퇴원 복구의 raw `setDoc` 두 곳 → uid 발급 / 아카이브에서 복원
> - 관리앱 로더 `migrateStudentUid()` — uid 없는 학생을 매 실행 시 메꾸는 마지막 안전망
> - 학생앱: 로그인 객체에 uid 포함 + 세션 캐시 갱신 시 uid 주입(재로그인 없이 치유)

### 4단계 — 문서ID 재작성 (되돌리기 어려움)

새 ID로 문서를 복사하고 옛 문서를 지운다. 컬렉션 단위로 나눠서, 한 번에 하나씩.

권장 순서(영향 작은 것부터):
`student_memos`(13) → `planner_comments`(456) → `planner_ai_reviews`(500) →
`planners`(506) → `daily_reports`(527) → `penalties`(142) → `schedules`/`schedule_base`(각 50) →
`students`(54) ← **마지막**

`students`가 마지막인 이유: 문서ID가 바뀌는 순간 좌석 기반 폴백이 전부 무효가 된다.

⚠️ **`penalties`·`merits`는 규칙상 delete가 금지되어 있다.** 옛 문서를 지울 수 없으므로
규칙을 한시적으로 열거나, 옛 문서를 `migrated:true`로 표시해 조회에서 제외하는 방식을 택한다.
후자를 권장한다(규칙을 여는 순간이 가장 위험하다).

### 5단계 — Storage 이관 + 정리

- `planners/{좌석}/` → `planners/{uid}/` 복사 후 옛 경로 삭제
- 좌석 기반 폴백 코드 제거
- 2026-08-04 핫픽스(이름 검증)도 이때 걷어낸다 — uid 도입으로 불필요해진다
- `_swapSeatDocs`가 단순해진다: `students.seat` 필드 두 개만 맞바꾸면 끝

---

## 5. 손대야 하는 코드

### 관리앱 `saebom_schedule_with_hours.html` (18,000행)

| 위치 | 내용 |
|---|---|
| `_studentDocData` / `_saveSeatDoc` | 좌석 문서 조립 — uid 기준으로 |
| `_swapSeatDocs` | **대부분 삭제 가능해진다** (seat 필드 교환만 남음) |
| `_renderPenaltyList` / `resetPenalties` | seatKey 조회 → uid |
| `_renderMeritModalBody` | 이름 비교 → uid |
| `saRender` (학습분석) | `where('seat')` → `where('uid')` |
| 새벽 2시 리셋 / `promoteWeekly` 연동 | 좌석 기준 순회 → uid |
| 이름 변경 핸들러 | **이관 로직 전체 삭제** (이름이 키가 아니게 됨) |

### 학생앱 `saebom_student.html`

벌점·상점·학습분석·플래너(달력/상세/업로드)·일일보고 제출. Storage 경로도 여기.

### 학부모앱 `saebom_parent.html`

벌점·상점·학습분석. 로그인이 **이름 완전일치 + 학부모 전화 뒷4자리**라 이름 의존이 남는다 →
uid 도입 후에도 로그인은 별도 검토 필요(이름을 못 없앤다).

### Functions `functions/index.js`

플래너 AI 검사 경로가 좌석 기준이다: `planner_ai_reviews/{좌석}_{날짜}`,
`planners/{좌석}_{날짜}`, `students/{좌석}`, 배치 `custom_id`.
**배치 요청이 진행 중일 때 이관하면 결과가 엉뚱한 문서에 쓰인다** → 배치가 빈 시간대에 작업할 것.

### `firestore.rules`

좌석 기반 검증(`penalties`의 `seatKey` 필수 등)을 uid 기준으로 교체.
규칙 배포는 자동화가 실패하므로 **로컬 `firebase.cmd`로 수동 배포**해야 한다.

---

## 6. 위험과 대응

| 위험 | 대응 |
|---|---|
| 이관 중 학생이 입실/퇴실 → 기록 유실 | **운영시간 밖에 작업**(전원 퇴실 22:00 이후, 새벽 2시 리셋 전) |
| 플래너 AI 배치 진행 중 이관 | 배치 pending 0 확인 후 착수 |
| 3앱 배포 시차로 읽기/쓰기 불일치 | 3단계는 3앱 동시 배포. 4단계 전까지 좌석 폴백 유지 |
| `penalties`/`merits` delete 금지 | 옛 문서를 지우지 말고 `migrated:true` 마킹 |
| 동명이인 1건 오배정 | 자동 판별 금지. 수동 지정 후 해당 학생 기록 육안 검수 |
| 되돌리기 | 0단계 PITR + 단계별 export. 4단계 전까지는 필드 삭제만으로 원복 |
| 다른 세션의 동시 편집 | 저장소에 다른 작업이 진행 중일 수 있다. 커밋 전 `git status` 확인 |

---

## 7. 검증 방법

각 단계 후 REST로 전수 대조한다(로그인 불필요, 읽기 전용 — 이번 조사에 쓴 방식).

1. **건수 보존**: 이관 전후 컬렉션별 문서 수가 같은가
2. **귀속 정확도**: 모든 기록의 `uid`가 이름과 일치하는가 (불일치 0이어야 함)
3. **고아 기록**: uid가 어떤 신원과도 매칭되지 않는 기록이 있는가
4. **집계 일치**: 학생별 누적 공부시간·상벌점 합계가 이관 전후 동일한가
5. **화면 확인**: 좌석을 물려받은 5명(좌석 13·27·36·37·40)에서 남의 기록이 안 보이는가

3~5번은 자동 스크립트로 만들어 매 단계 실행한다.

---

## 8. 범위 밖 (별도 처리)

- **`push_tokens` 403건** — 이 저장소에 코드가 없다. FCM 푸시 배포본을 따로 조사해야 한다.
- **Storage 규칙** — 저장소에 없고 콘솔에서만 관리 중. 확인 후 이 문서에 반영할 것.
- **학부모 로그인의 이름 의존** — uid로 못 없앤다. Auth 도입(상용화 1단계)과 함께 다룬다.
- **`feedback_events` 35건** — 조인 근거 부족. uid 없이 두고 후속 정리.

---

## 9. 착수 전 결정할 것

1. **4단계까지 갈 것인가, 3단계에서 멈출 것인가.**
   3단계까지만 해도 좌석 교환 오염은 사라진다. 문서ID 재작성(4단계)은 이득이 "키가 깔끔해진다"뿐이라
   상용화 일정이 급하면 미뤄도 된다. **권장: 3단계에서 일단 멈추고 운영하며 관찰.**
2. **작업 시간대.** 22:00 전원 퇴실 이후 ~ 새벽 2시 리셋 이전 구간.
3. **동명이인 1건의 신원 확정** — 원장 확인 필요.
